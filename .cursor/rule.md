---
description: قوانین و قراردادهای پروژهٔ مراقبه (muraqebe) — اپ تک‌صفحه‌ای React با Vite، React Router و Tailwind.
alwaysApply: true
---

# 🧭 قوانین کلی

- از **React** با **TypeScript** استفاده شود.
- برای UI از **Tailwind CSS** و آیکون‌ها از **@heroicons/react** (۲۴/outline و ۲۴/solid) استفاده شود.
- ثابت‌ها و تنظیمات مشترک در `app/constants.ts` و تایپ‌ها در `app/types.ts` قرار گیرند.
- توابع کمکی مشترک در `app/lib/` تعریف شوند.

# 📁 ساختار پروژه

پروژه یک اپ واحد در پوشهٔ `app/` است (مونورپو یا Turborepo ندارد).

| مسیر | کاربرد |
|------|--------|
| `app/App.tsx` | نقطهٔ ورود اپ، مسیریابی و لایهٔ اصلی state |
| `app/index.tsx` | رندر روت و ثبت PWA |
| `app/pages/` | صفحه‌های مسیرها (TodayPage، CalendarPage، StatsPage، SettingsPage، CoursesPage) |
| `app/components/` | کامپوننت‌های قابل استفاده مجدد |
| `app/lib/` | توابع کمکی (مثلاً `dateUtils`) |
| `app/constants.ts` | ثابت‌ها (TASKS، RAMADAN_DAYS، CITIES، SESSIONS و غیره) |
| `app/types.ts` | تایپ‌ها و اینترفیس‌های TypeScript |

**Alias مسیر:** از `@/` برای رفرنس به روت اپ استفاده شود (مثلاً `@/constants`، `@/types`، `@/pages`، `@/components`، `@/lib/dateUtils`).

# 🧱 توسعهٔ کامپوننت و صفحات

- کامپوننت‌های قابل استفاده مجدد در `app/components/` قرار گیرند و در صورت نیاز از `app/pages/` استفاده شوند.
- صفحات در `app/pages/` تعریف و از `app/App.tsx` با `<Routes>` و `<Route>` استفاده شوند.
- کامپوننت‌ها ریسپانسیو و تا حد ممکن در دسترس (accessibility) باشند.

# ⚛️ State و هوک‌ها

- برای state داخل کامپوننت‌ها از React hooks استفاده شود.
- هوک‌های سفارشی در `app/hooks/` قرار گیرند و با پیشوند `use` نام‌گذاری شوند.
- از کتابخانهٔ state مدیریت خارجی فقط در صورت نیاز واقعی استفاده شود.

# 🧭 مسیریابی

- مسیرها در `app/App.tsx` با **React Router** (`Routes`, `Route`) تعریف شوند.
- برای لینک‌ها از `NavLink` یا `Link` از `react-router-dom` استفاده شود.
- مسیرهای فعلی: `/`, `/calendar`, `/stats`, `/settings`, `/courses`.

# 🎨 استایل

- استایل با **Tailwind CSS** و کلاس‌های utility اعمال شود.
- از کلاس‌های اینلاین Tailwind استفاده شود؛ از فایل‌های CSS جدا یا MUI استفاده نشود.
- برای انیمیشن‌های ساده می‌توان از کلاس‌های `transition-*` یا استایل‌های inline با `@keyframes` (مثلاً در `<style>`) استفاده کرد.

# 📁 نام‌گذاری فایل و پوشه

- پوشه‌ها و نام فایل‌های غیرکامپوننت: **kebab-case**.
- فایل‌های کامپوننت و صفحه: **PascalCase** (مثلاً `SettingsPage.tsx`, `PrayerTimes.tsx`).
- کامپوننت‌های React با **PascalCase** نام‌گذاری شوند.

# 🛠 ابزارها و قراردادها

- **Bun**: اسکریپت‌ها و نصب پکیج با Bun انجام شود (نه npm، pnpm یا yarn).
- **Vite**: بیلد و dev سرور با Vite (`bun run dev`, `bun run build`).
- **PWA**: اپ با `vite-plugin-pwa` به صورت PWA پیکربندی شده است.
- **RTL**: برای متن و فرم‌های فارسی از `dir="rtl"` یا `dir="ltr"` متناسب با محتوا استفاده شود.

# 📝 یادداشت‌ها

- کد تمیز و تا حد امکان مستند نگه داشته شود.
- از alias `@/` برای ایمپورت از روت اپ استفاده شود تا مسیرها خواناتر بمانند.
